package com.roomwise.ObservePayments;

// Interface to define the Observer behavior
public interface Observer {
    void updatePaymentStatus(boolean status); // Method for observers to update payment status
}
